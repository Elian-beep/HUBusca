import { Button, Text, View } from "react-native";

export const Profile = () => {

    return (
        <View>
            <Text>Perfil selecionado</Text>
            <Button title="clicar" />
        </View>
    );
}

export default Profile;